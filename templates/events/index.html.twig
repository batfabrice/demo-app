{% extends 'base.html.twig' %}

{% block title 'Events' %}

{% block body %}
    <h1>{{ events|length }} Events</h1>

    <ul>
        {% for event in events %}
            <li>{{ event }}</li>
        {% endfor %}
    </ul>
{% endblock %}
